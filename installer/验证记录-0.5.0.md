# 0.5.0 Windows 通用安装版验证

- 插件 Release 构建：成功，0 warnings、0 errors；Assembly 0.5.0.0，原 GUID 保持不变。
- 核心模型、贴图、单位与尺寸推断：131 项通过。
- Rhino 原生界面、大小窗口、裁剪、PBR 材质与放置：160 项通过。
- 可移植路径测试：5 项通过，覆盖中文/空格、UTF-16 配置、相对路径拒绝、系统盘默认路径、旧版便携目录。
- 隔离 Standard 安装：中文与空格路径，程序/数据分离，成功。
- 隔离 Full 组件升级：成功；安装后的精简运行库完成本地图片识别。
- 从实际安装路径重新执行 131 项检查：通过。
- Update 更新：保留 Node、识别组件、原 DataRoot 和模型；产生旧插件备份。
- Update 用于新目录：正确拒绝，退出码 7。
- Rhino 正在运行时更新：正确拒绝，退出码 7，未结束 Rhino 进程。
- 卸载：移除程序与对应测试注册；模型、settings.json、last-asset.json 保留。
- 作者现有 0.4.6 → 0.5.0 迁移：成功；程序/数据均保持 D:\assetcopilot；设置和任务记录 SHA256 未变。
- 真实启动入口：自动启动 Rhino，打开标题为 Remy Asset Copilot 0.5.0 的插件窗口。
- 中文安装向导人工画面检查：目录、Rhino、数据目录页面可读，旧数据位置在升级时锁定保留。
- 发布源码：73 个文件，约 3.24 MiB，不含程序二进制、模型权重、个人数据或检测到的 API Key；源文件与当前项目一致。

发布：Standard 25,941,652 bytes；Full 174,415,907 bytes；Update 3,401,301 bytes。SHA256 见 releases/SHA256SUMS.txt。

边界：上述验证在作者本机完成，未在第二台/物理无 D 盘的 Windows 机器上实测；真实 API 生成本次未重复调用。安装包尚无商业代码签名。

测试日志在 work/installer-qa 和 work/qa；隔离 QA 程序已卸载，正式 0.5.0 安装保留。测试过程中未改变用户 API 凭据。
