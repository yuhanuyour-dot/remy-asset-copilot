# 0.5.2 普通 Rhino 启动兼容验证

- 固定 RhinoCommon 8.0 SDK；发布 net48 通用兼容程序集，源码保留 net7.0-windows 构建目标。
- Framework 与 Core 编译均成功；GitHub 源码导出独立编译成功，均 0 warnings / 0 errors。
- .NET Framework 4.8 核心检查：134 项通过（含真实本地识别、压缩 GLB、中文路径/带引号参数、取消与超时）。
- Rhino 8.35 Framework 4.8、.NET 7.0.1、.NET 8.0.24：同一兼容代码各通过 161 项界面/材质/尺寸/放置检查。
- Rhino 真实插件加载器可加载正式 0.5.2 RHP 并打开窗口，Framework 模式通过；保留原插件 GUID 与 AssetCopilot 命令。
- 启动器行为检查通过：仅请求 /runscript="_AssetCopilot"，不指定运行时；5 项数据路径检查通过。
- 安装器保留运行进程保护：Rhino 正在运行时退出码 7，不替换插件。
- Full / Standard / Update 已生成；源码与发布清单不含用户配置、模型、密钥或 Rhino SDK。

安装后实际验证记录见下方。当前开发机 Rhino 8.0 原版未安装，最低宿主兼容性依据为 8.0 SDK，仍建议外部试装。测试 SDK 会提示 net7 已结束支持，插件不要求用户切换到旧运行时。安装包未代码签名。

日志：开发机器 work/universal-0.5.2。备份：backups/before-0.5.2 与 backups/github-upload-before-0.5.2。

## 安装后直接命令验收（2026-09-17）

- 在所有 Rhino 已关闭时，用正式 Update 安装包从 0.5.1 更新到 0.5.2，安装成功，程序集 0.5.2.0。
- 未指定 /netcore 或 /netfx，保留原有 netfx 全局设置，正常启动 Rhino；直接执行 AssetCopilot，成功打开 Remy Asset Copilot 0.5.2 窗口。
- 同一已安装 RHP 在 .NET 7.0.1 与 .NET 8.0.24 中串行测试直接命令打开，均成功；每轮测试前确认无 Rhino 进程。
- 三次均从 D:\assetcopilot\AssetCopilot\dist\AssetCopilot.rhp 加载正式插件，而非测试程序集；无须先手动加载 RHP。
- DataRoot 保持 D:\assetcopilot，settings.json、last-asset.json 校验值未变；没有更改 Rhino 全局运行时。
- Full / Standard / Update 与 SHA256SUMS-0.5.2.txt 校验一致；本次未向 GitHub 发布。
